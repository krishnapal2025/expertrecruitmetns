# AWS Configuration
# Copy this file to .env and fill in the values

# AWS region
AWS_REGION=us-east-1

# Application Name
APPLICATION_NAME=rh-job-portal

# Environment (production, staging, development)
ENVIRONMENT=production

# EC2 Instance Type (t2.micro, t2.small, t2.medium, etc.)
EC2_INSTANCE_TYPE=t2.micro

# RDS Instance Type (db.t3.micro, db.t3.small, db.t3.medium, etc.)
RDS_INSTANCE_TYPE=db.t3.micro

# Database Credentials
DB_USERNAME=admin
DB_PASSWORD=ChangeMe123!
DB_NAME=rh_job_portal

# S3 Bucket Name (must be globally unique)
# S3_BUCKET_NAME=your-unique-bucket-name

# CloudFront Distribution Name
# CLOUDFRONT_DISTRIBUTION_NAME=your-cloudfront-distribution-name

# Git Repository URL (optional - used for EC2 instance to clone the repository)
GIT_REPO_URL=https://github.com/yourusername/your-repo.git